<main class="main">

  <%= render 'boardheader', board: @board %>

  <section class="section_links container_preview_sortable" id="linksPage" data-url="<%= sort_links_path %>">
    <%= render 'links/newlink' %>
    <%= render @links %>
  </section>

  <div id="loadMoreBoards"></div>

</main>
